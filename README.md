This is a next js application.
steps to start -> 
1. clone the repo
2. npm install
3. add a .env file
4. npm start or npm run dev
   
.env file structure
{
MONGODB_URI="mongo_api_key"
JWT_SECRET="my_secret"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
GEMINI_API_KEY="your_gemini_key"
}
